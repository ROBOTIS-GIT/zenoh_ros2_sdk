# CLAUDE.md

Claude Code should treat `AGENTS.md` as the source of truth for this repository.
Read it before making code, docs, release, or workflow changes.

Repository-specific Claude skills are available in `.claude/skills/`:

- `bump-version`
- `add-repository`
- `create-example`

Keep `.claude/skills/` aligned with `.agents/skills/` when changing shared
agent workflows.
